doctype html
html
    head
        meta(charest='utf-8')
    body
        h1
            a(href='/topic') Server Side JavaScript
        ul
            each item in topics
                li
                    a(href='/topic/'+item.id)= item.title
        block content