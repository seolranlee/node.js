doctype html
html
    head
        meta(charest='utf-8')
    body
        h1
            a(href='/topic') Server Side JavaScript
        ul
            each topic in topics
                li
                    a(href='/topic/'+topic)= topic
        article
            h2= title
            p= description
        //if title
        //    article
        //        h2= title
        //        p= description
        div
            a(href='/topic/new') new